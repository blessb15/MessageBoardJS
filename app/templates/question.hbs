{{question-detail question=model destroyQuestion="destroyQuestion"}}
<h2 class="question-page">Answers:</h2>
  {{#each model.answers as |answer|}}
  <h5>{{answer.solution}}</h5>
  <h3>-{{answer.name}}</h3>
  {{/each}}
<br>
{{new-answer question=model saveAnswer="saveAnswer"}}
