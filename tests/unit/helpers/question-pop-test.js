import { questionPop } from 'message-board/helpers/question-pop';
import { module, test } from 'qunit';

module('Unit | Helper | question pop');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionPop([42]);
  assert.ok(result);
});
